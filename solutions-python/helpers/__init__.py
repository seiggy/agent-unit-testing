"""Test helper package (SOLUTION reference).

Python-idiomatic equivalents of the .NET test helpers:

* :mod:`helpers.foundry_config` — env / connection-string → judge model config
  (parity with ``FoundryConnectionStringParser.cs`` / ``FoundryConnectionStringParts``).
* :mod:`helpers.agent_run` — ``agent.run()`` result → evaluator inputs
  (parity with ``AgentRunResponseExtensions.ToChatResponse``).
"""
